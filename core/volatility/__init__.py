"""Options-volatility analytics (pure compute).

Modules here take raw market data in and return plain data structures out — no
I/O and no plotting. ``iv_surface`` is the first module; ``smile``, ``greeks``,
``term_structure`` and others are intended to slot in alongside it.
"""
